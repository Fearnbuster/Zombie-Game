// Sends updates for all of the replica objects that need updating:

// argument0 - ID of the queue that holds all of the replica
// objects that need to be updated.

packet = buffer_create(1*MB, buffer_grow, 1);


