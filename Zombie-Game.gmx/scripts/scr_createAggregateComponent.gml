// This component pertains to "aggregate" objects, which are objects
// that:
// (1: Consist of sub-objects,
// and/or
// (2: Is a sub-object of a aggregate object.

// --------------------------
// Inject variables into caller:
mIsAggregate = false; // creation only - bool 
mParentObject = 0; // creation only - u32
mRelativePositionX = 0.0; // creation only - f32
mRelativePositionX = 0.0; // creation only - f32

// Arm angle = arm object's "image_angle" ~ float32

// --------------------------
return obj_destructibleComponent;
